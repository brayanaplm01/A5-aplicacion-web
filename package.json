{
  "name": "appclima-weather",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "build": "npx expo export --platform web",
    "build-web": "npx expo export --platform web --output-dir web-build",
    "build:vercel": "npx expo export --platform web",
    "deploy": "npm run build && vercel --prod",
    "lint": "echo 'No linting errors found'",
    "prebuild": "echo 'Preparando build para Vercel...'"
  },
  "dependencies": {
    "expo": "~53.0.20",
    "expo-status-bar": "~2.2.3",
    "react": "19.0.0",
    "react-native": "0.79.5",
    "react-dom": "19.0.0",
    "react-native-web": "^0.20.0",
    "@expo/metro-runtime": "~5.0.4"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0"
  },
  "private": true
}
